using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SkillFiller : MonoBehaviour
{
    Image _filler;
    Skill _skill;
    
    

    void Start()
    {
        _filler = GetComponent<Image>();  
        //_skill=
        
    }

    // Update is called once per frame
    void Update()
    {
       // _filler.fillAmount = _skill.
    }
}
